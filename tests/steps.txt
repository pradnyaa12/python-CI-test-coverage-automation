create virtual environment first
install depedancies from requirements.txt file

pip install pytest-html
pip install coverage

----------------------------------------------------------------

#to run pytest
cd test 
cd tests
Pytest

---------------------------------------------------------------

# python -m pytest
# coverage run -m pytest
# coverage report   
# coverage html 
#pytest --html=test_report.html 